apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: find-placement-schools-temp.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
  - host: find-placement-schools-temp.teacherservices.cloud
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: find-placement-schools-production
            port:
              number: 80
