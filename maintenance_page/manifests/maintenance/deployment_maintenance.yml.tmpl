apiVersion: apps/v1
kind: Deployment
metadata:
  name: find-placement-schools-maintenance
  labels:
    app: find-placement-schools-maintenance
spec:
  replicas: 2
  selector:
    matchLabels:
      app: find-placement-schools-maintenance
  template:
    metadata:
      labels:
        app: find-placement-schools-maintenance
    spec:
      nodeSelector:
        teacherservices.cloud/node_pool: applications
        kubernetes.io/os: linux
      containers:
      - name: find-placement-schools-maintenance
        image: ghcr.io/dfe-digital/find-placement-schools-maintenance:#MAINTENANCE_IMAGE_TAG#
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: 64M
            cpu: 0.1
          limits:
            memory: 64M
            cpu: 1
